export enum ActionEnum {
  package_search = "package_search",
}

export enum FiltersEnum {
  tags = 'tags',
  organization = 'organization',
  groups = 'groups',
  res_format = 'res_format',
  resource_license_en = 'resource_license_en'
}
